server:
  port: 8080

spring:
  datasource:
    url: jdbc:mariadb://magamsale-database.clo060iiyf0u.ap-northeast-2.rds.amazonaws.com:3306/magamsale
    username: admin
    password: magamsale8282
    driver-class-name: org.mariadb.jdbc.Driver
    hikari:
      # 🔥 [핵심] 주차 공간 제한하기
      # 기존에는 이 설정이 없어서 혼자서 100개씩 쓰려고 했을 겁니다.
      # DB가 작으므로 10~15개 정도로 낮춰서 서로 양보해야 합니다.
      maximum-pool-size: 10

      # 최소 유지 연결 수 (너무 많이 잡지 마세요)
      minimum-idle: 2

      # 연결 수명 (아까 경고 떴던 거 해결용)
      max-lifetime: 180000
      connection-timeout: 30000


  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  # [수정됨] 아래에 있던 servlet과 main 설정을 여기(spring 아래)로 통합했습니다.
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  main:
    allow-bean-definition-overriding: true

# [수정됨] cloud는 spring 밖으로 빼야 Java 코드(@Value)에서 읽을 수 있습니다. (맨 앞으로 당김)
cloud:
  aws:
    credentials:
      access-key: AKIA4L6SACQ2EV6KIP4Q
      secret-key: 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
    s3:
      bucket: magamsale-images-bucket
    region:
      static: ap-northeast-2
    stack:
      auto: false

jwt:
  signing:
    key: AZERndAHhy4KjO5f81HWmbgIiZgOGJQH1L1o2p8pjD8RbIyKWrrpUMx8KkcFUfGI7T6waYHS7iZ3TBb4l9KwsG

kakao:
  api:
    key: c755a9eb4b18f0c4e49148e3810b5ed9

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.magamsale.store.entity